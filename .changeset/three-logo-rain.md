---
"create-newt-app": patch
---

Port the docs homepage logo rain from SVG + d3 to a three.js WebGL scene, keeping the same look and behavior: theme-aware silhouettes, background circles, size-based opacity, and size-based speed (small chips fast, large slow).
