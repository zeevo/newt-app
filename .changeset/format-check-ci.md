---
"create-newt-app": patch
---

Scaffolded apps now ship an empty `.prettierrc`, so formatting follows prettier's defaults the way `create-turbo` and `create-next-app` do, instead of the `singleQuote` style inherited from `nest new`.
