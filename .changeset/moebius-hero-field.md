---
"@newt-app/ui": minor
---

Add a `.field` utility — a saturated Incal-blue color field, identical in light and dark — and apply it to the homepage hero. `.field` rebinds the semantic tokens (`--background`, `--foreground`, `--border`, `--accent`, `--code`) on its own subtree, so descendants invert onto the field without per-element overrides.
