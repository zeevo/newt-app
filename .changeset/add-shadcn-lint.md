---
"create-newt-app": minor
---

`--shadcn` scaffolds register `@shadcn/lint` with the selected linter and enable `no-restyle`, `no-raw-colors`, `no-arbitrary-values` and `require-static-classes`; the example pages no longer restyle design-system components
