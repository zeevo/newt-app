---
"create-newt-app": patch
---

Relabel the first "Learn more" link on the generated landing page from "Documentation" to "newt-app". The site has had no docs since the docs section was removed, so the label promised something that does not exist; the link still points at newt-app.com, and the label now matches its siblings, which are all names rather than descriptions.
