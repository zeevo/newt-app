---
"@newt-app/ui": minor
---

Carry the saturated field across the whole site, docs included. `.field` now rebinds the sidebar, muted, popover and primary tokens plus the `--color-*` theme aliases, so shiki code blocks, inline code chips and the docs sidebar all invert correctly. The field stays theme-responsive — brighter cerulean in light, deeper indigo in dark — so the mode toggle still does something.
