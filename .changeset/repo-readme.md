---
"create-newt-app": patch
---

Replace the default Turborepo starter README with a concise newt-app README (Getting Started, Documentation, What's inside). The root `README.md` is a symlink to `packages/create-newt-app/README.md`, so the repo and the npm package share one source, mirroring how Next.js is set up.
